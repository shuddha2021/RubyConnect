# frozen_string_literal: true

require_relative 'lib/database'

Database.setup
Database.seed!
puts "✓ Database ready — #{Database.user_count} users seeded."
puts "  Run: ruby app.rb"
puts "  Open: http://localhost:4567"
